# Pandly — Environment Variables
# Copy to .env.local and adjust values.

# Domain for metadata / OpenGraph. Set to the real production domain before launch.
NEXT_PUBLIC_SITE_DOMAIN="pandly.mockup.local"
